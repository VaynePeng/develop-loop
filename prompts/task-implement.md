# Task IMPLEMENT 阶段

严格按当前任务的锁定设计、allowedPaths 和 PLAN 产物实现。保留用户已有改动，不处理本任务范围外的问题。

完成后按 `implementation-report.md` 记录实际修改、偏差和待验证项，保存到 `.develop-loop/tasks/<task-id>/`，再用自包含运行时的 `task pass --artifact <文件>` 提交。
