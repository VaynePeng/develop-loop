# Task PLAN 阶段

只规划 `next.context.task` 对应的当前任务。读取锁定系分和任务计划，确认依赖已完成，列出本任务要修改的文件、实现步骤、风险和逐条验收方式。

本阶段不得修改业务代码。按 `task-stage-plan.md` 保存到 `.develop-loop/tasks/<task-id>/` 后，用自包含运行时的 `task pass --artifact <文件>` 提交。
